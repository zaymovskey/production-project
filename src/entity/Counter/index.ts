import { counterReducer } from './model/slice/counterSlice';
import type { ICounterScheme } from './model/types/CounterScheme';
import { Counter } from './ui/Counter';

export { counterReducer, Counter, type ICounterScheme };
